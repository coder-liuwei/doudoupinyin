---
description: Git 分支与合并流程 — 功能分支开发，合并/推送 main 须用户确认
alwaysApply: true
---

# Git 工作流

## 分支

- 功能改动在**独立功能分支**上完成（如 `feat/xxx`），**不要直接在 `main` 上改代码**。
- 从 `main` 切分支前确保工作区干净；每阶段可在功能分支上单独 commit。

## 合并 main 前（必须停住）

完成开发并跑通门禁后，**停止并告知用户**，等待其本地验证：

```bash
npm test
npm run build
# 用户自行 npm run dev 验 UI
```

**禁止**在用户明确确认前执行以下任一操作：

- `git checkout main && git merge …`
- `git push origin main`
- 任何把功能分支合入 `main` 的操作

用户可能要先看效果、再决定是否合并。自动化测试通过 ≠ 可以合并。

## 用户确认后才可执行

仅当用户**明确说**可以合并（如「可以合并 main」「验过了，合并吧」）时：

```bash
git checkout main
git merge <feature-branch>
npm test && npm run build   # main 上再验一次
```

合并后**仍须等用户确认**再 `git push origin main`（本项目 push 到 main 会触发 Vercel 部署）。

## 禁止

- **不要** force push `main`
- **不要**未经用户要求就 push 到远端
- **不要**把「计划完成 / 测试通过」当作合并或推送的许可
